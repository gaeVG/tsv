// Declarations
import { PolyZoneType } from '@declares/zone';
import { EntranceType } from '@declares/entrance';

type SocietyBuildingType = {
  zone: PolyZoneType;
  entrances: EntranceType[];
};

export { SocietyBuildingType };
